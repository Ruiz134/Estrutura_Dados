[project]
name = "estrutura-dados"
version = "0.1.0"
description = "Projeto de estrutura de dados"
authors = [
    {name = "Alan Ruiz"}
]
license = "Não"
requires-python = "3.11.3"
dependencies = [
]

[tool.poetry]
package-mode = false

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
